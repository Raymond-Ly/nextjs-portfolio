import type { NextPage } from 'next'
import { Template } from '../template/Template';

const Home: NextPage = () => {
  return (
    <Template>
      <section>
      </section>
    </Template>
  )
}

export default Home
