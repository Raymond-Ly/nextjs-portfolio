### Portfolio to learn Next.js

## Getting Started

1. Install dependencies: `yarn`
1. Start local: `yarn dev` on http://localhost:3000

## Linting

Run `yarn lint` to view warnings/errors. Add `--fix` to fix.
